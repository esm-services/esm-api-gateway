spring:
  application:
    name: api-gateway-server
  profiles:
    active:
    - prod
  cloud:
    config:
      discovery:
        enabled: true
      username: config-server
      password: config-server
      fail-fast: true
encrypt:
  key: ABCDEFGHIJKLMNOPQRSTUVWXYZ